// 已废弃：模型配置改为「能力编辑」（agent_model_capability_sheet.dart），
// 原生成参数(温度/思考档等)调参 UI 已移除。本文件无任何 import 引用，可物理删除。
//
// 说明：AgentModelParams 数据类与请求构造层（translation_service 的 thinking
// 翻译）仍保留——现在永远取默认值，等价于「回落服务商默认」。
